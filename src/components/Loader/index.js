import React from "react";

import "./style.scss";

/**
 * It returns a div with the text "Veuillez patienter" and the class "loading"
 * @returns A React component.
 */
const Loader = () => {
  return <div className="loading">Veuillez patienter</div>;
};

export default Loader;
